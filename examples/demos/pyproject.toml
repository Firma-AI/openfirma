[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "firma-demos"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "openai-agents",
    "httpx",
    "python-dotenv",
]

[tool.hatch.build.targets.wheel]
packages = ["agent"]
