# Firma Run Examples

This directory contains examples and local workflows for running agents through
`firma run`.

- `local/` contains local setup helpers, profile config templates, capability
  renewal helpers, and related setup documentation.
- `e2e/` contains the Linux structural E2E harness for `firma run`.

These examples are intentionally kept outside `docs/` so each workflow carries
its script, documentation, and assets together.
