# Self-contained config for the macOS VZ basic curl example.

[authority]
listen_addr = "127.0.0.1:0"
policy_dir = "policies"
issuance_policy_dir = "issuance-policies"
revocation_file = "examples/firma-run/macos-vz-basic-exec/.runtime/revocations.txt"
key_file = ".runtime/authority.key"
max_ttl = "1h"
bundle_ttl_seconds = 30

[sidecar]
mode = "enforce"

[sidecar.interceptor.https_mitm]
enabled = false

[sidecar.policy]
dir = "policies"

[sidecar.mapping]
rules_path = "mapping-rules.toml"
default_protected = true

[sidecar.capability_validation]
clock_skew_tolerance_seconds = 0

[sidecar.connector]
default_timeout_ms = 120000

[sidecar.audit]
sink = "file"
file_path = ".runtime/audit.jsonl"
signing_key_path = ".runtime/audit.key"

[run]
profile = "generic"

[run.profiles.generic]
backend = "vz"
