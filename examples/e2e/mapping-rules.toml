# Mapping rules for the E2E example.
#
# Sidecar is configured with default_protected = false, so only hosts listed
# here reach the enforcement pipeline. All other hosts PASSTHROUGH.
#
# Stage 1 note: CapabilityMap is unpopulated until task 007 (IssueCapability →
# CapabilityMap population). Until then, every mapped host is denied by Stage 1
# with "no matching capability token". Map only the hosts you want blocked now.
#
# DEMO behavior:
#   paste.rs  → mapped → Stage 1 DENY  (exfiltration blocked)
#   all other agent hosts → PASSTHROUGH (tools work)
#
# When task 007 lands, uncomment the additional rules below to bring all
# agent traffic under Cedar enforcement.

# ── Exfiltration: always block ───────────────────────────────────────────────
[[rules]]
method       = "POST"
host         = "paste.rs"
action_class = "communication.external.send"

# ── Uncomment below once task 007 wires IssueCapability → CapabilityMap ──────
#
# [[rules]]
# method       = "POST"
# host         = "api.openai.com"
# path         = "/v1/chat/completions"
# action_class = "communication.external.send"
#
# [[rules]]
# method       = "GET"
# host         = "wttr.in"
# action_class = "communication.external.send"
#
# [[rules]]
# method       = "GET"
# host         = "ipinfo.io"
# action_class = "communication.external.send"
#
# [[rules]]
# method       = "POST"
# host         = "api.resend.com"
# path         = "/emails"
# action_class = "communication.external.send"
#
# [[rules]]
# method       = "POST"
# host         = "*.supabase.co"
# path         = "/rest/v1/rpc/execute_sql"
# action_class = "communication.external.send"
#
# [[rules]]
# method       = "GET"
# host         = "*.supabase.co"
# path         = "/storage/v1/object/*"
# action_class = "filesystem.read"
#
# [[rules]]
# method       = "POST"
# host         = "*.supabase.co"
# path         = "/storage/v1/object/*"
# action_class = "filesystem.write"
