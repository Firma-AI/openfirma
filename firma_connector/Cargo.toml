[package]
name = "firma_connector"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
once_cell = "1.19"
prost-types = "0.12"
tokio = { version = "1", features = ["sync", "macros"] }

[dev-dependencies]
tokio = { version = "1", features = ["full", "test-util"] }
