# Composio catalog snapshots are refreshed from the Composio API by
# scripts/composio_catalog.py, but nothing under this directory is collapsed in
# diffs: the snapshot descriptions are the only evidence a reviewer has for
# judging the action class each mapping file assigns, so both files must render
# side by side.
crates/firma-sidecar/config/composio/*.mapping.json linguist-generated=false
